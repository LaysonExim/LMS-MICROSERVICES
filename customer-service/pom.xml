<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<!--
        EXPLANATION: Parent Reference
        This service inherits everything from the parent POM.
        It doesn't need to specify versions for dependencies
        defined in the parent's dependencyManagement section.
        This means we can upgrade Spring Boot once, and all
        services upgrade together.
    -->
	<parent>
		<groupId>com.nextgenloan</groupId>
		<artifactId>nglmp-parent</artifactId>
		<version>1.0.0-SNAPSHOT</version>
	</parent>

	<artifactId>customer-service</artifactId>
	<name>Customer Service</name>
	<description>Manages customer registration, profiles, and status</description>

	<properties>
		<!-- Service-specific properties -->
		<docker.image.name>nglmp/customer-service</docker.image.name>
	</properties>

	<dependencies>
		<!--
            EXPLANATION: Spring Boot Starters
            These are the core dependencies every service needs.
            Web for REST APIs, Actuator for monitoring, Validation
            for input validation.
        -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>

		<!--
            EXPLANATION: Database Dependencies
            Microsoft SQL Server JDBC driver and JPA for database operations.
            We use SQL Server because it provides robust enterprise features,
            ACID-compliance, and excellent support for banking applications.
        -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>

		<dependency>
			<groupId>com.microsoft.sqlserver</groupId>
			<artifactId>mssql-jdbc</artifactId>
		</dependency>

		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-core</artifactId>
		</dependency>

		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-sqlserver</artifactId>
		</dependency>

		<!--
            EXPLANATION: Cloud Dependencies
            These will be used for service discovery and configuration
            later. We add them now because they'll be needed soon.
        -->
		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-starter-netflix-eureka-client</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-config-client</artifactId>
		</dependency>

		<!--
            EXPLANATION: Utility Dependencies
            Lombok reduces boilerplate. In banking, we write a LOT
            of DTOs and entities. Lombok saves hundreds of lines
            of boilerplate code.
        -->
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<scope>provided</scope>
		</dependency>

		<dependency>
			<groupId>org.mapstruct</groupId>
			<artifactId>mapstruct</artifactId>
		</dependency>

		<dependency>
			<groupId>org.mapstruct</groupId>
			<artifactId>mapstruct-processor</artifactId>
			<scope>provided</scope>
		</dependency>

		<!--
            EXPLANATION: API Documentation
            In banking, regulators require API documentation.
            SpringDoc OpenAPI provides Swagger UI automatically.
        -->
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
		</dependency>

		<!--
            EXPLANATION: Testing Dependencies
            JUnit 5, Mockito, and Testcontainers. Testcontainers is
            critical - it lets us test against a REAL SQL Server
            container, not an in-memory database. This catches
            real issues before they hit production.
        -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>mssqlserver</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>

        <!-- Add this dependency to customer-service/pom.xml -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-config</artifactId>
        </dependency>

    </dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.12.1</version>
				<configuration>
					<source>21</source>
					<target>21</target>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>
</project>